{
  "version": 2,
  "builds": [
    {
      "src": "api/app.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install && prisma generate"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)", 
      "dest": "/api/app.js"
    }
  ]
}
